from utils.classifier import classify_prompt, classify_modestus


# print(classify_prompt("who will learn best for literature"))
print(classify_prompt(
    prompt="thêm task mới: gặp sếp từ 12h trưa tới 1h chiều ngày mai"
))
